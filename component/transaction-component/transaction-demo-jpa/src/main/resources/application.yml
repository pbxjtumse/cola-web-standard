spring:
  datasource:
    url: jdbc:h2:mem:tx_jpa;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

iron:
  transaction:
    enabled: true
