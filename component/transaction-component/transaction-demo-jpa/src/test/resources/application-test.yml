spring:
  datasource:
    url: jdbc:h2:mem:tx_jpa;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false

iron:
  transaction:
    enabled: true
