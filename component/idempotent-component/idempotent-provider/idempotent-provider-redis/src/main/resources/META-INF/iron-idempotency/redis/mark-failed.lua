-- ARGV owner,version,failureCode,failureMessage,retryable,nowMs,ttlMs
local key=KEYS[1];local function h(n)local v=redis.call('HGET',key,n);if not v then return '' end;return v end;local function snap(code)return {tostring(code),h('namespace'),h('key'),h('request_hash'),h('status'),h('owner_token'),h('version'),h('result_payload'),h('failure_code'),h('failure_message'),h('failure_retryable'),h('processing_expire_at'),h('created_at'),h('updated_at'),h('completed_at')} end
if redis.call('EXISTS',key)==0 then return {'2'} end;if h('status')~='PROCESSING' then return snap(4) end;if h('owner_token')~=ARGV[1] or h('version')~=ARGV[2] then return snap(3) end;redis.call('HSET',key,'status','FAILED','failure_code',ARGV[3],'failure_message',ARGV[4],'failure_retryable',ARGV[5],'processing_expire_at','','updated_at',ARGV[6]);redis.call('PEXPIRE',key,tonumber(ARGV[7]));return snap(1)
