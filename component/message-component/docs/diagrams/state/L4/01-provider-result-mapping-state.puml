@startuml
!include ../../_common/style.puml

title L4：Provider 原生结果到统一发送状态映射

state "Native Provider Result" as NATIVE #FFEBEE
state "CONFIRMED" as CONFIRMED #DCFCE7
state "FAILED" as FAILED #FEE2E2
state "UNKNOWN" as UNKNOWN #FEF3C7
state "REJECTED" as REJECTED #FFE4E6

[*] --> NATIVE
NATIVE --> CONFIRMED : success ack / send ok
NATIVE --> FAILED : network error / client error
NATIVE --> UNKNOWN : timeout / flush timeout / uncertain result
NATIVE --> REJECTED : serialization / authorization / invalid topic
CONFIRMED --> [*]
FAILED --> [*]
UNKNOWN --> [*]
REJECTED --> [*]
@enduml
